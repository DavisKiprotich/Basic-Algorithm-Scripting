function confirmEnding(str, target) {
  let targetMatch = str.slice(str.length - target.length,str.length);
  console.log(targetMatch);
  if(targetMatch == target){
    return true
  }
  return false
}

confirmEnding("Bastian", "n");
